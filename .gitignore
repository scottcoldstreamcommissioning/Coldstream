node_modules/
.env
backend/output/
*.log

# Real site photos used for local testing must never be committed —
# they may contain confidential client/site information.
test-photos/
/photos/

# Persisted storage — original photo bytes + SQLite metadata. Real site
# data, never committed; also where local test runs accumulate.
backend/data/
