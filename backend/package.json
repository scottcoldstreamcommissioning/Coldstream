{
  "name": "capture-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capture backend — Claude vision pipeline + storage for Coldstream site photos.",
  "scripts": {
    "prove": "node scripts/prove-pipeline.js",
    "ingest": "node scripts/ingest.js",
    "list-photos": "node scripts/list-photos.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "sharp": "^0.33.5"
  }
}
