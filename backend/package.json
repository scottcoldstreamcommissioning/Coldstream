{
  "name": "capture-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Capture backend — Phase 1: prove the Claude vision pipeline on real site photos.",
  "scripts": {
    "prove": "node scripts/prove-pipeline.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "sharp": "^0.33.5"
  }
}
